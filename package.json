{
  "scripts": {
    "clean:repo": "node scripts/clean_repo.js --prune",
    "clean:repo:dry": "node scripts/clean_repo.js"
  },
  "dependencies": {
    "@prisma/client": "^7.10.0",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2"
  }
}
